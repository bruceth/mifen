import { Tuid, Action, Query, Map } from 'tonva';

export interface UQs {
    mi: {
        // 股票: Tuid;
        // Tag: Tuid;
        // TagStock: Map;
        // SaveTag: Action;
        // AllTags: Query;
        // UserSettings: Map;
        // StockWarning: Map;
        // StockWarningAll: Query;
        // TagsStockCount: Query;
    }
}
